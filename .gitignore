# --- Node ---
node_modules/

# --- macOS / OS junk ---
.DS_Store
Thumbs.db

# --- Editors / IDE ---
.vscode/
.idea/
*.swp

# --- Logs / debug ---
*.log
npm-debug.log*

# --- TypeScript build remnants (safety; this package builds plain JS) ---
*.tsbuildinfo

# --- Demo screen recordings (448 MB total; keep out of the repo) ---
demo/

# NOTE: lib/ is intentionally NOT ignored — the bundle ships its built
# outputs (lib/index.js + lib/client.js) so git/tarball installs work
# without running `node build.js` first (see AGENTS.md).
